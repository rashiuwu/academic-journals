// Save inside folder 'shapeArea'
package shapeArea;

public class Triangle {
    // Method to calculate area of triangle
    public double area(double base, double height) {
        return 0.5 * base * height;
    }
}
